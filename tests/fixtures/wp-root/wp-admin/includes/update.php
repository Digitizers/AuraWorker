<?php
/**
 * Test-only placeholder.
 *
 * class-aura-worker-updater.php require_once's this path unconditionally
 * (as WordPress core itself does), so it must exist for a unit test to reach
 * that code without a fatal "failed to open stream". The real function/class
 * definitions live in tests/bootstrap.php, loaded before any test runs — this
 * file is intentionally empty.
 *
 * @package Aura_Worker\Tests
 */
